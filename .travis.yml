sudo: required

services:
  - docker

language: scala

cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot

script:
  - sudo mkdir .sbt
  - docker-compose run book npm install
  - docker-compose run book ./sbt.sh pdf

